strings = ['case', 'book', 'picture', 'buddha'];

function capitaliseWords(){
    var string = strings.map();
    // string.toUppercase();
    console.log(string);
    return string;
}

capitaliseWords();
